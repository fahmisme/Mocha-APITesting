const testCase = {
    description: 'Get User',
    positive: {
        case1: 'Get user with valid data'
    },
    negative: {
        case1 : 'Get user with invalid data'
    }
}

module.exports = {
    testCase
}